### To run in backend
1. Navigate to the backend directory 
    ```sh
    cd website/server
    ```
4. Install dependencies 
    ```sh
    npm install
    ```
2. Run server
    ```sh
    node backend.js
    ```
